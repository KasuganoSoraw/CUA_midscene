# air-tickets-demo

这是 `browser-demo` 场景中的 Qatar Airways 航班搜索任务。

- `midscene-flow.json`：人、Agent 与未来前端共同修改的唯一长期执行流程。
- `task.json`：任务说明和运行时输入绑定，不保存输入默认值。
- `SKILL.md`：Agent 调用和长期修改规则。
- `source/`：本任务的 trace、处理日志和截图证据。
- `reports/`：运行快照与执行结果，不纳入 Git。
