# Tasks

## 1. 服务端删除边界

- [x] 1.1 实现用户任务目录删除服务，验证只允许唯一解析的可写 user 任务且保留场景目录
- [x] 1.2 实现原始录制目录删除服务，验证目标严格位于录制根一级目录且活动录制期间拒绝删除
- [x] 1.3 注册任务与录制 DELETE 接口，验证成功响应和错误状态可由现有 API 错误链路消费

## 2. 工作台交互

- [x] 2.1 扩展前端 API 客户端并验证任务和录制删除请求使用正确的编码路径与 HTTP 方法
- [x] 2.2 实现可复用的双语应用内确认对话框，验证取消不会触发删除且键盘和焦点语义可用
- [x] 2.3 在任务复核和录制详情中接入删除入口，验证只读/忙碌状态受保护且删除后刷新选择与草稿

## 3. 验证与文档

- [x] 3.1 增加服务端删除成功、只读拒绝、路径越界、目标不存在和活动录制保护测试并运行 review 测试
- [x] 3.2 增加中英文案和前端 API 测试并运行完整 execution 测试与 Web 构建
- [x] 3.3 更新分层使用文档并执行 OpenSpec strict 校验
