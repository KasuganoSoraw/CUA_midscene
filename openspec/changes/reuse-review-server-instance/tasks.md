## 1. 服务身份与复用

- [x] 1.1 增加默认端口常量、数据根指纹和只读 review 服务身份接口
- [x] 1.2 在默认启动路径中探测并复用协议与数据根一致的 `127.0.0.1:47831` 服务
- [x] 1.3 为端口空闲、竞态复用和不匹配占用实现明确的启动结果与错误

## 2. CLI 与文档

- [x] 2.1 让 `cua review` 输出 created/reused 状态并保持 `--no-open`、`--json` 契约
- [x] 2.2 更新 README、Skill 和主帮助说明，说明固定端口与单实例复用边界

## 3. 验证

- [x] 3.1 补充 server 与 CLI 测试，覆盖首次启动、重复复用、数据根不匹配和显式 `port: 0`
- [x] 3.2 运行类型检查、完整测试、生产构建和 OpenSpec strict 校验
- [x] 3.3 在本地真实启动两次 `cua review`，确认 URL 相同且只新增一个监听进程
