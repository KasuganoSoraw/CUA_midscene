## 1. Review 数据契约

- [x] 1.1 为 ReviewStep 增加录制目标小图与参考图片投影，并从 processed log 和点击/双击 Flow 提取对应字段
- [x] 1.2 保持参考图本地路径受任务 `source/` 边界约束，并覆盖服务组合测试

## 2. 语义编辑器

- [x] 2.1 解析字符串点击动作和 `action: null + locate.prompt + locate.images` 两种标准结构
- [x] 2.2 构建带图点击/双击 Flow 时保留图片名称、URL、prompt 和等待时间，并覆盖双向测试

## 3. Vue 复核界面

- [x] 3.1 在草稿重建后同步参考图片投影，并为步骤列表增加独立参考图标识
- [x] 3.2 在证据查看区增加执行参考图入口、缩略卡片、名称与路径说明
- [x] 3.3 保证高级 JSON 和普通表单修改后参考图展示与 Flow 预览同步
- [x] 3.4 步骤切换时优先默认展示面向人类复核的局部图，并保留参考图标签
- [x] 3.5 移除服务端和前端草稿重建中的上一录制步骤 contextEvidence 回退
- [x] 3.6 为没有自身局部图和全局图的步骤展示统一默认占位图，并覆盖证据归属测试

## 4. 验证

- [x] 4.1 运行 TypeScript/Vue 类型检查、完整测试与生产构建
- [x] 4.2 运行 OpenSpec strict 校验并复核工作区变更范围
